# 复制一份为 .env 或在 shell rc 里导出
# runner 自动读环境变量，不读 .env（如需自动读请安装 python-dotenv 并在代码里加载）

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# 可选：覆盖默认数据路径
# ZXF_SRC_DIR=/path/to/zxftrans
# ZXF_OUT_DIR=/path/to/zxftrans_structured
